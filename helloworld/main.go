package main

import (
	"fmt"
)

func main() {
	fmt.Println("Hello there!")
}

/*
--Go cli definitions--
Go run is used to build and execute.
Go build is used generate an executable
Go fmt is used to format all the code in each in the current dir
Go install compiles and installs a package
Go get downlods the raw source code of someone else's package
Go test will run any tests associated with the current project
*/
